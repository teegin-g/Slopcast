---
phase: 02-content-migration-and-data-tables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/slopcast/WellsTable.tsx
  - src/components/slopcast/WellsTable.test.tsx
  - src/components/slopcast/FilterChips.tsx
  - src/components/slopcast/hooks/useTableFilters.ts
  - src/components/slopcast/DesignWellsView.tsx
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "User can see the well list in a table with columns: checkbox, name, formation, lateral length, status, operator"
    - "User can sort wells by clicking any column header"
    - "User can filter wells via a search bar and dropdown filters for status and formation"
    - "User can select/deselect individual wells and select-all via checkbox column"
    - "Selecting wells in the table highlights them on the map, and clicking map wells checks them in the table"
    - "Active filters appear as removable chips below the search bar"
    - "User can resize columns by dragging column borders"
    - "Table header stays sticky while scrolling the body"
  artifacts:
    - path: "src/components/slopcast/WellsTable.tsx"
      provides: "TanStack-powered wells table with sort, filter, select, resize"
      min_lines: 150
    - path: "src/components/slopcast/WellsTable.test.tsx"
      provides: "Unit tests for wells table rendering, sorting, filtering, selection"
      min_lines: 50
    - path: "src/components/slopcast/FilterChips.tsx"
      provides: "Removable filter chip display component"
      min_lines: 20
    - path: "src/components/slopcast/hooks/useTableFilters.ts"
      provides: "Filter state persistence hook surviving section switches"
      min_lines: 30
  key_links:
    - from: "src/components/slopcast/WellsTable.tsx"
      to: "useSlopcastWorkspace"
      via: "selectedWellIds prop + onRowSelectionChange callback"
      pattern: "onSelectWells|selectedWellIds"
    - from: "src/components/slopcast/WellsTable.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable with sort/filter/select/resize/core row models"
      pattern: "useReactTable"
    - from: "src/components/slopcast/DesignWellsView.tsx"
      to: "src/components/slopcast/WellsTable.tsx"
      via: "Replaces GroupWellsTable import"
      pattern: "WellsTable"
---

<objective>
Install TanStack Table and Framer Motion dependencies, then build the wells table as a TanStack Table migration from the existing hand-rolled GroupWellsTable.

Purpose: Replace the hand-rolled wells table with TanStack Table to get sortable, filterable, resizable columns with checkbox row selection synced bidirectionally with the map.
Output: Working WellsTable component integrated into DesignWellsView, with filter state that persists across section navigation.
</objective>

<execution_context>
@/Users/teegingroves/.claude/get-shit-done/workflows/execute-plan.md
@/Users/teegingroves/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-migration-and-data-tables/02-CONTEXT.md
@.planning/phases/02-content-migration-and-data-tables/02-RESEARCH.md
@.planning/phases/01-styling-foundation-and-app-shell/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types.ts:
```typescript
export interface Well {
  id: string;
  name: string;
  lat: number;
  lng: number;
  lateralLength: number;
  status: 'PRODUCING' | 'DUC' | 'PERMIT';
  operator: string;
  formation: string;
}
```

From src/hooks/useSidebarNav.ts:
```typescript
export type Section = 'wells' | 'economics' | 'scenarios';
export function useSidebarNav(): { section: Section; setSection: (s: Section) => void };
```

From src/hooks/useSlopcastWorkspace.ts (relevant selection state):
```typescript
// Returned from useSlopcastWorkspace:
selectedWellIds: Set<string>
handleToggleWell: (id: string) => void
handleSelectWells: (ids: string[]) => void
handleSelectAll: () => void
handleClearSelection: () => void
```

From src/components/slopcast/GroupWellsTable.tsx (existing, being replaced):
```typescript
export interface GroupWellsTableProps {
  wells: Well[];
  selectedWellIds: Set<string>;
  onToggleWell: (id: string) => void;
  // ... plus filters, sort state
}
function formatFeet(value: number) { /* formats lateral length */ }
function compareValues(a: Well, b: Well, key: SortKey) { /* sort comparator */ }
```

From src/components/layout/AppShell.tsx:
```typescript
// section comes from useSidebarNav, content renders based on workspace.designWorkspace
// Children render inside <main className="flex-1 overflow-y-auto">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create filter persistence hook + FilterChips</name>
  <files>package.json, src/components/slopcast/hooks/useTableFilters.ts, src/components/slopcast/FilterChips.tsx</files>
  <action>
1. Install both Phase 2 dependencies:
   ```bash
   npm install @tanstack/react-table motion
   ```
   If `motion` package fails, fall back to `npm install framer-motion`. Verify import resolves with a quick check.

2. Create `src/components/slopcast/hooks/useTableFilters.ts`:
   - A hook that stores column filter state (TanStack ColumnFiltersState) and global search string
   - State must persist across AnimatePresence unmount/remount. Use useRef at a higher level OR store in URL search params (consistent with existing useSidebarNav pattern). Recommended: use URL search params for filters — `?section=wells&q=permian&status=PRODUCING&formation=Wolfcamp`
   - Export: `useTableFilters(tableId: string)` returning `{ globalFilter, setGlobalFilter, columnFilters, setColumnFilters, activeFilters: Array<{id, value, label}>, removeFilter(id) }`
   - `activeFilters` is a derived array for rendering chips — maps columnFilters to display-friendly objects
   - Filter state keyed by tableId so wells and cash flow tables have independent filters

3. Create `src/components/slopcast/FilterChips.tsx`:
   - Props: `{ filters: Array<{id: string, value: string, label: string}>, onRemove: (id: string) => void }`
   - Renders a horizontal flex row of chips, each with label text and an X button
   - Style: `rounded-full px-2 py-0.5 text-[10px] bg-theme-surface2/60 text-theme-text border border-theme-border/40` with hover state on X
   - Only renders if filters array is non-empty
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Both packages installed and importable. useTableFilters hook created with URL-param-backed persistence. FilterChips renders removable chip tags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build WellsTable with TanStack Table and integrate into DesignWellsView</name>
  <files>src/components/slopcast/WellsTable.tsx, src/components/slopcast/WellsTable.test.tsx, src/components/slopcast/DesignWellsView.tsx</files>
  <behavior>
    - Renders 5 data columns (name, formation, lateral length, status, operator) plus checkbox select column
    - Clicking a column header sorts ascending, clicking again sorts descending, third click removes sort
    - Typing in search bar filters across all columns (global filter)
    - Dropdown filters for status (PRODUCING/DUC/PERMIT) and formation filter visible rows
    - Checkbox column: individual toggle + select-all header checkbox
    - Row selection state syncs bidirectionally with external selectedWellIds Set
    - Active filters show as removable chips
    - formatFeet formats lateral length values
  </behavior>
  <action>
1. Create `src/components/slopcast/WellsTable.test.tsx`:
   - Test: renders all 5 column headers plus checkbox
   - Test: renders well rows with correct data (use 3-4 mock wells)
   - Test: clicking column header triggers sort state change
   - Test: global filter filters rows by text
   - Test: checkbox select toggles row selection
   - Test: selectedWellIds prop correctly marks rows as selected (external -> table sync)

2. Create `src/components/slopcast/WellsTable.tsx`:
   - Props interface: `{ wells: Well[], selectedWellIds: Set<string>, onSelectWells: (ids: string[]) => void, onToggleWell: (id: string) => void }`
   - Use `useReactTable` with: `getCoreRowModel`, `getSortedRowModel`, `getFilteredRowModel`, column resizing enabled (`columnResizeMode: 'onChange'`)
   - **CRITICAL**: Set `getRowId: (row) => row.id` to keep selection stable across sort/filter
   - Column definitions: checkbox (size 40, no sort/resize), name (200), formation (140), lateralLength (100, cell uses formatFeet), status (100), operator (160)
   - Bidirectional selection sync per RESEARCH.md Pattern 5:
     - Table -> workspace: `onRowSelectionChange` converts TanStack row selection to well IDs, calls `onSelectWells`
     - Workspace -> table: derive `rowSelection` Record from `selectedWellIds` Set using well.id
   - Search bar: `<input>` above table, wired to `table.setGlobalFilter`
   - Dropdown filters: status and formation `<select>` elements beside search bar
   - Integrate `useTableFilters('wells')` for persistent filter state
   - Render FilterChips below search bar for active filters
   - Table wrapper: `rounded-panel border shadow-card bg-theme-surface1/70 border-theme-border overflow-hidden`
   - Header: `sticky top-0 z-10 bg-theme-surface1 border-b border-theme-border` with `text-[10px] font-black uppercase tracking-[0.24em] text-theme-cyan`
   - Sort indicators: arrow characters in header (up/down/none)
   - Body rows: `text-[11px] text-theme-text` with hover `bg-theme-surface2/30`, selected row highlight
   - Use `table-fixed` layout with inline width styles from TanStack column sizing
   - Column resize: render resize handle div on header cells, style as a thin draggable border
   - Sticky header with scrollable body via `overflow-auto max-h-[60vh]` on table container

3. Update `src/components/slopcast/DesignWellsView.tsx`:
   - Replace `import GroupWellsTable` with `import WellsTable`
   - Replace `<GroupWellsTable ...>` usage with `<WellsTable wells={...} selectedWellIds={...} onSelectWells={...} onToggleWell={...} />`
   - Remove props that were specific to GroupWellsTable's internal sort/filter (those are now internal to WellsTable)
   - Keep the `isClassic` branching pattern if GroupWellsTable had it — check before removing
  </action>
  <verify>
    <automated>npx vitest run src/components/slopcast/WellsTable.test.tsx && npm run typecheck && npm run build</automated>
  </verify>
  <done>Wells table renders with all 5 columns plus checkbox. Sorting works on all columns. Global search and dropdown filters work with removable chips. Bidirectional selection sync with map via selectedWellIds. Columns are resizable. Header is sticky. Build passes.</done>
</task>

</tasks>

<verification>
- `npm test` passes (all existing + new WellsTable tests)
- `npm run typecheck` passes
- `npm run build` succeeds without errors
- Wells table visible in app at localhost:3000/?section=wells
</verification>

<success_criteria>
- Well list renders in TanStack Table with sortable, filterable, resizable columns
- Checkbox selection syncs bidirectionally with map well selection
- Filter state persists when navigating away from wells section and back
- Glass design system styling applied (rounded-panel, theme colors, sticky header)
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-migration-and-data-tables/02-01-SUMMARY.md`
</output>
