---
phase: 01-styling-foundation-and-app-shell
plan: 04
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - src/components/layout/Sidebar.tsx
  - src/components/layout/AppShell.tsx
  - src/components/layout/Sidebar.test.tsx
autonomous: true
requirements:
  - NAV-01
gap_closure: true

must_haves:
  truths:
    - "Theme selector appears only in the PageHeader, not in the sidebar"
    - "Sidebar renders without theme-related props"
  artifacts:
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Sidebar without theme selector JSX or theme props"
      contains: "SidebarGroupTree"
    - path: "src/components/layout/AppShell.tsx"
      provides: "AppShell no longer passes theme props to sidebar"
      contains: "sidebarProps"
  key_links:
    - from: "src/components/slopcast/PageHeader.tsx"
      to: "theme system"
      via: "ThemeSwitcher component already in header"
      pattern: "ThemeSwitcher|setThemeId"
---

<objective>
Remove the duplicate theme selector from the Sidebar component. The user reported that the theme selector should only be accessible in the PageHeader (top header bar), not the sidebar bottom. The PageHeader already has a full ThemeSwitcher component, so the sidebar copy is redundant.

Purpose: Fix UAT issue #5 -- theme selector location
Output: Sidebar without theme selector, AppShell without theme prop threading to sidebar
</objective>

<execution_context>
@/Users/teegingroves/.claude/get-shit-done/workflows/execute-plan.md
@/Users/teegingroves/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-styling-foundation-and-app-shell/01-02-SUMMARY.md
@.planning/debug/sidebar-theme-selector.md

@src/components/layout/Sidebar.tsx
@src/components/layout/AppShell.tsx
@src/components/layout/Sidebar.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove theme selector from Sidebar and clean up prop threading</name>
  <files>src/components/layout/Sidebar.tsx, src/components/layout/AppShell.tsx, src/components/layout/Sidebar.test.tsx</files>
  <action>
**Sidebar.tsx changes:**
1. Remove lines 18-20 from SidebarProps interface: `themeId?`, `onSetThemeId?`, `themes?` -- these 3 optional props only exist for the sidebar theme selector
2. Remove the corresponding destructured props from the function signature (lines within the destructuring block)
3. Remove the entire theme selector JSX block at sidebar bottom (lines 109-132): the `{!collapsed && themes && onSetThemeId && themeId && (...)}` block including its preceding divider

**AppShell.tsx changes:**
1. Remove lines 116-118 from the sidebarProps object: `themeId: workspace.themeId`, `onSetThemeId: workspace.setThemeId`, `themes: workspace.themes` -- these are no longer consumed by Sidebar

**Sidebar.test.tsx changes:**
1. Remove any `themeId`, `onSetThemeId`, or `themes` props from test render calls if present
2. Remove any test cases that specifically test the theme selector in the sidebar (e.g., "renders theme icons when expanded")
3. If no theme-specific tests exist, no changes needed to the test file

Do NOT touch PageHeader.tsx -- it already has the correct ThemeSwitcher and should remain unchanged.
  </action>
  <verify>
    <automated>npm run build && npm run typecheck && npm test</automated>
  </verify>
  <done>Theme selector only appears in PageHeader. Sidebar has no theme-related props or JSX. All builds and tests pass.</done>
</task>

</tasks>

<verification>
- `npm run build && npm run typecheck && npm test` all pass
- Sidebar.tsx has zero references to themeId, onSetThemeId, or themes
- AppShell.tsx sidebarProps object has zero theme-related entries
- PageHeader.tsx ThemeSwitcher is unchanged
</verification>

<success_criteria>
- Theme selector is only accessible via PageHeader, not sidebar
- No TypeScript errors from removed props
- Existing sidebar tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-styling-foundation-and-app-shell/01-04-SUMMARY.md`
</output>
